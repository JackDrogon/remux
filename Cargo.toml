[package]
name = "retmux"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
authors = ["c"]

[lints.rust]
unused_must_use = "deny"

[profile.dev]
debug = 1

[profile.release]
lto = "thin"
codegen-units = 1

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
